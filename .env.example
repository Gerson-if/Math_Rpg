# Copy this file to .env for local development.
FLASK_ENV=development
SECRET_KEY=change-me-to-a-random-value

# Leave unset locally to fall back to SQLite (instance/dev.db).
# In production this must be a postgresql:// URL.
# DATABASE_URL=postgresql://user:password@localhost:5432/math_rpg
